<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PawCare" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <!-- Tema principal -->
    <style name="Theme.PawCare" parent="Base.Theme.PawCare" />

    <!--<style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_paw_logo</item>
        <item name="android:windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.PawCare</item>
    </style>-->
</resources>